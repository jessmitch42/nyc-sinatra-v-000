<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>All Figures</title>
  </head>
  <body>
    <p>All Figures: </p>

    <% @figs.each.with_index(1) do |f, i| %>
      <p><%= i %>. <a href="/figures/<%= f.id %>"><%= f.name %></a></p>
    <% end %>
  </body>
</html>
